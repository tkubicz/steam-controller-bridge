[package]
name = "bridge-runtime"
version = "1.2.0"
edition.workspace = true
license.workspace = true
# Not published to crates.io; distribution is GitHub releases only.
publish = false
repository.workspace = true

[dependencies]
bridge-core = { path = "../bridge-core" }
bridge-output = { path = "../bridge-output" }
controller-mapper = { path = "../controller-mapper" }
desktop-bindings = { path = "../desktop-bindings" }
gamepad-state = { path = "../gamepad-state" }
recording = { path = "../recording" }
serde_json.workspace = true
steam-controller-device = { path = "../steam-controller-device" }
steam-controller-protocol = { path = "../steam-controller-protocol" }

[lints]
workspace = true
