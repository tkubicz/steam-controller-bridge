[package]
name = "sc-bridge"
version = "1.10.0"
edition.workspace = true
license.workspace = true
# Not published to crates.io; distribution is GitHub releases only.
publish = false

[dependencies]
bridge-core = { path = "../../crates/bridge/bridge-core" }
clap.workspace = true
bridge-output = { path = "../../crates/bridge/bridge-output" }
bridge-runtime = { path = "../../crates/bridge/bridge-runtime" }
virtual-gamepad = { path = "../../crates/host/virtual-gamepad" }
ctrlc.workspace = true
desktop-bindings = { path = "../../crates/app/desktop-bindings" }
recording = { path = "../../crates/app/recording" }

[lints]
workspace = true
