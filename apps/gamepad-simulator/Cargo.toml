[package]
name = "gamepad-simulator"
version = "1.10.0"
edition.workspace = true
license.workspace = true
# Not published to crates.io; distribution is GitHub releases only.
publish = false

[dependencies]
bridge-output = { path = "../../crates/bridge/bridge-output" }
clap.workspace = true
gamepad-state = { path = "../../crates/bridge/gamepad-state" }
virtual-gamepad = { path = "../../crates/host/virtual-gamepad" }
recording = { path = "../../crates/app/recording" }

[lints]
workspace = true
